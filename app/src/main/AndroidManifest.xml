<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 
    PERMISSIONS: Required for accessing music files 
    -->
    
    <!-- 
    READ_EXTERNAL_STORAGE: For Android 12 and below
    Allows app to read files from device storage
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- 
    READ_MEDIA_AUDIO: For Android 13+ (API 33+)
    New permission for reading audio files (more specific than READ_EXTERNAL_STORAGE)
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnionMusicPlayer"
        tools:targetApi="31">
        
        <!-- 
        MAIN ACTIVITY: Entry point of the app 
        -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UnionMusicPlayer">
            
            <!-- 
            Intent Filter: Makes this activity the app launcher 
            -->
            <intent-filter>
                <!-- Action: This is the main entry point -->
                <action android:name="android.intent.action.MAIN" />
                
                <!-- Category: This activity should appear in the app launcher -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
